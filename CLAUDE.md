# PEAKOFF

예측 기반 혼잡 회피 여행 플래너. 2026 관광데이터 활용 공모전 ②-2 웹·앱 구현 부문 출품작.

한국관광공사 관광지 집중률 예측 및 연관 관광지 데이터를 결합해, 여행 일정의 혼잡도를 사전에 진단하고 '날짜를 바꾸거나' 또는 '장소를 바꾸는' 두 가지 회피 경로를 제시하여, 한적한 대안지 중심의 여행 코스를 완성하는 서비스.

- **지정과제**: 2번 — 유명 관광지 쏠림으로 인한 오버투어리즘(과밀화) 문제
- **개발 마감**: 2026-09-21 16:00 (실질 마감 9/20 — 마감일 배포 금지)
- **팀**: 2인 (개발 1, 데이터 분석 겸 PM 1)

---

## 절대 규칙 (위반 시 공모전 탈락/감점)

아래는 협상 불가. 코드 작성 시 항상 우선한다.

1. **공사 OpenAPI는 실시간 호출한다. 로컬 DB에 저장/적재하지 않는다.**
   - 심사 시 인증키로 실제 API 호출 이력을 검증한다. 호출 로그가 쌓여야 한다.
   - 매 추천 요청마다 실제 API를 호출할 것.
   - 성능이 필요하면 짧은 인메모리 캐시(수 분)만 허용. 공격적 캐싱·영구 저장 금지.
2. **파일 데이터(Excel, CSV) 다운로드 활용 금지.** 반드시 OpenAPI 호출 형태여야 인정된다.
3. **폐기 예정 코드 사용 금지.** 지역/분류 지정은 반드시 **법정동 코드 / 신분류 코드** 사용.
   - 사용 금지: 구 `지역코드 조회`, 구 `서비스 분류코드 조회` (26년 폐기 예정)
4. **서비스 내에 "한국관광공사", "KTO", 공사 로고를 사용하지 않는다.**
   - 출처 표기가 필요하면 "공공데이터 기반" 등 중립적 표현 사용.
5. **사용자 GPS 위치를 서버로 전송하지 않는다.**
   - 위치기반서비스사업자 신고 이슈 회피. 지역은 사용자가 직접 입력한다.
   - 지도에 "현재 위치" 기능 넣지 말 것.

---

## 기술 스택

- **백엔드**: Java + Spring Boot (개발자 담당)
- **프론트엔드**: 웹, **모바일 우선 반응형** (390px 기준 설계, 데스크톱에서 깨지지 않게)
- **분석**: Python / Jupyter Notebook (팀원 담당, 검증용)
- **앱 개발 안 함.** 웹만. 앱은 스토어 등록·승인 기한 리스크로 제외.

### 분석 로직 처리 방식
팀원의 파이썬 노트북은 **검증용 연구실**이고, 서비스에 직접 붙지 않는다.
노트북에서 확정된 **규칙(임계값, 점수식, 정렬 기준)을 자바로 이식**해서 서버에 심는다.
별도 파이썬 API 서버를 띄우지 않는다. (규칙 기반 로직이므로 불필요)

---

## 활용 공사 OpenAPI (전부 실시간 호출)

| 데이터 | 역할 |
|---|---|
| 관광지 집중률 방문자 추이 예측 | 날짜별 방문자 비율 예측 → 혼잡 등급의 원천 |
| 관광지별 연관 관광지 정보 (티맵 기반) | 대안지 후보 (중심 관광지와 연결성 높은 곳) |
| 지역별 방문자수 빅데이터 | 지역 단위 쏠림 검증·보조 지표 |
| 국문 관광정보 (TourAPI) | 장소 상세·좌표(mapx/mapy)·이미지 등 화면 재료 |

> ⚠️ 이 데이터는 **실시간이 아니라 예측·통계값**이다.
> UI 문구와 코드 주석에서 "실시간 혼잡"이 아니라 **"예상 혼잡 / 예측 기반"**으로 표현할 것.

외부 API(지도 등)는 자유롭게 추가 가능.

---

## 서비스 화면 흐름

```
1. 여행 입력 (지역 · 기간 · 방문 시점)
      ↓
   사용자 선택 ──┬── 2. 혼잡 예보 (선택)
                 └── 바로 코스 추천
      ↓
3. 코스 초안 (일자별 슬롯 자동 구성)
      ↓
4. 슬롯별 교체 — 날짜 바꾸기 / 장소 바꾸기
      ↓
5. 최종 확정 + 비교 (지도 + 인기순 코스 대비 혼잡 지수 절감)
      ↓
6. 코스 저장
      ↑
   (4~5 사이 재조정 루프 존재)
```

### 핵심 도메인 로직 (분석가 설계 → 자바 이식)
1. **혼잡 등급화** — 예측 원시값을 붐빔/보통/한적 3단계로 변환 (임계값 + 요일·계절 보정)
2. **대안지 매칭** — `매칭점수 = 연관성 점수 × (1 − 정규화 혼잡도)` (초안, 검증 후 조정)
3. **코스 구성** — 일자×시간대 슬롯 채우기. **핵심 명소는 무조건 배제하지 않는다** (핵심 명소는 유지하되 한산한 시간대 추천, 나머지 슬롯을 대안지로 교체). 순서는 근접도 기반 정렬 정도면 충분 (최단경로 최적화 불필요)
4. **비교 지표** — 같은 조건으로 "인기순 코스"(혼잡 무시, 인기순 정렬)와 "회피 코스"를 생성해 **예상 혼잡 지수** 비교
   - 절대 방문자 수가 아님. 반드시 "예상 혼잡 지수"로 명명하고 산출 근거를 밝힐 것
5. **추천 근거 노출** — 모든 추천에 이유를 함께 표시 (예: "함께 많이 찾는 곳 · 예상 혼잡 낮음")

---

## 인증 / 저장 — 단계적 구현

**순서를 반드시 지킬 것.** 아래 층이 완성되기 전에 위층으로 가지 않는다.

- **1층 (최우선)**: 로그인 없이 도는 추천 루프 완성. 저장은 **localStorage**.
- **2층**: 이메일/비밀번호 로그인 + JWT. 게스트가 만든 코스를 로그인 시 계정으로 승격.
- **3층 (여유 시)**: 소셜 로그인(카카오/네이버). 외부 등록·검수 시간 소요로 후순위.

게스트로도 서비스 전체를 사용할 수 있어야 한다. 로그인은 선택이며 진입 장벽이 되어선 안 된다.
코드는 처음부터 사용자 식별 부분을 분리해, 나중에 인증을 얹기 쉽게 설계할 것.

---

## 범위 밖 (만들지 말 것)

시간이 한정적이므로 아래는 명시적으로 제외한다. 요청받지 않는 한 제안하지도 말 것.

- 블루/그린 무중단 배포, Docker 오케스트레이션
- Prometheus/Grafana 모니터링, 부하 테스트(k6)
- Materialized View 등 고급 DB 최적화
- 마이크로서비스 분리
- 사용자 실시간 위치(GPS) 기능

배포는 "심사위원이 URL로 접속해 안정적으로 사용 가능한 상태"면 충분하다.

---

## 저장소 구조

```
peakoff/
├── backend/            # Spring Boot
├── frontend/           # 웹 (모바일 우선 반응형)
├── analysis/           # 팀원 파이썬 노트북 (검증용)
├── .github/workflows/  # CI: 빌드 확인 위주
└── CLAUDE.md
```

백엔드는 도메인별 패키지 분리. 외부 API 연동은 별도 `external` 패키지로 분리할 것.
예: `congestion/`, `course/`, `place/`, `external/`, `global/`

---

## 작업 방식

- **한 번에 큰 덩어리를 만들지 말 것.** 기능 하나씩 작게 쪼개서 구현하고, 매번 실제로 동작을 확인한 뒤 다음으로 넘어간다.
- 코드를 작성한 뒤에는 **무엇을 왜 그렇게 짰는지 설명**할 것. 개발자가 최종 발표(PT)에서 구현 내용을 직접 설명해야 한다.
- 인증키·시크릿은 **환경변수**로 처리. 절대 코드에 하드코딩하거나 커밋하지 않는다.
- 기능이 하나 동작할 때마다 커밋한다. (개발 이력 자체가 심사 자료)

## 심사 기준 (판단이 필요할 때 이 배점을 기준으로)

**1차 심사**: 서비스 구현성 30 / 기획력 30 / 데이터 활용 적절성 20 / 발전성 20
**최종 심사(PT)**: 적정성 30 / 완성도 30 / 실용성 25 / 발표 15

기능 추가와 완성도 사이에서 고민될 때는 **완성도와 안정성**을 택한다.
"안정적으로 구동되는 완제품"이 평가의 핵심이다.
