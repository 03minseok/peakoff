# PEAKOFF

예측 기반 혼잡 회피 여행 플래너. 2026 관광데이터 활용 공모전 ②-2 웹·앱 구현 부문 출품작.

사용자가 직접 짠 여행 코스를 한국관광공사 데이터로 진단해, 붐빌 것으로 예측되는 장소를 한적한 대안지로 교체하도록 돕는 서비스.

- **지정과제**: 2번 — 유명 관광지 쏠림으로 인한 오버투어리즘(과밀화) 문제
- **개발 마감**: 2026-09-21 16:00 (실질 마감 9/20 — 마감일 배포 금지)
- **팀**: 2인 (개발 1, 데이터 분석 겸 PM 1)
- **대상 지역**: 경주 · 제주시 · 서귀포시. 공사가 자료를 주는 단위가 시군구라
  제주도는 두 지역으로 갈려 있다 (⚠️ 한라산과 성산일출봉을 한 코스에 못 담는다)
- **목표**: 대상(문화체육관광부 장관상)

---

## 절대 규칙 (위반 시 공모전 탈락/감점)

협상 불가. 코드 작성 시 항상 우선한다.

1. **공사 OpenAPI로 데이터를 가져온다. 파일 데이터나 DB 적재로 대체하지 않는다.**
   - 심사 시 인증키로 호출 이력을 검증하므로, 개발 기간 내내 실제 호출이 발생해야 한다.
   - 성능을 위한 인메모리 캐시는 허용한다.
      공사 데이터는 1일 1회 동기화된다 (국문 관광정보 03:30 이후,
      집중률·연관 관광지·빅데이터 08:30 이후). 갱신 주기가 하루이므로 TTL 6시간을 기본으로 한다.
   - 금지: 데이터를 DB에 영구 적재하고 API를 사실상 호출하지 않는 구조.
2. **파일 데이터(Excel, CSV) 다운로드 활용 금지.** OpenAPI 호출 형태만 인정된다.
3. **폐기 예정 코드 사용 금지.** 지역/분류 지정은 **법정동 코드 / 신분류 코드** 사용.
   - 사용 금지: 구 `지역코드 조회`, 구 `서비스 분류코드 조회` (26년 폐기 예정)
4. **서비스 내에 "한국관광공사", "KTO", 공사 로고 사용 금지.**
   - 출처 표기는 "공공데이터 기반" 등 중립적 표현.
5. **사용자 GPS 위치를 서버로 전송하지 않는다.**
   - 위치기반서비스사업자 신고 이슈 회피. 지역·장소는 사용자가 직접 선택.
   - 지도에 "현재 위치" 기능 넣지 말 것.

---

## 서비스 흐름

```
[진입] 로그인 / 회원가입  또는  게스트로 바로 이용
   ↓
1. 여행 조건 입력 — 지역(경주 · 제주시 · 서귀포시) · 여행 날짜 · 여행 기간
   ↓  (경주를 모르는 사용자는 설문 기반 추천으로 초안을 받을 수 있다)
   ↓
2. 코스 직접 편집 — 지도 기반으로 여행지를 순서대로 추가 (일자별)
   ↓
3. 코스 완성 → 진단 — 각 장소의 한적도 계산 및 표시
   ↓
4. 장소 교체 — 대안 추천 (추천도 순), 근거와 함께 제시
   ↓         + 날짜 대안 제시 (더 한적한 날짜)
   ↓
5. 최종 코스 총점 — 원안 대비 개선폭 비교
   ↓
6. 저장 — 회원: 계정 저장 후 다른 코스와 비교 / 게스트: 1회성 이용
```

### 핵심 설계 원칙

**첫 코스는 사용자의 의도를 존중한다.** 사용자가 처음 짠 코스에는 추천도를 반영하지 않는다.
시스템이 개입하는 시점은 **진단(3단계)과 교체 추천(4단계)**부터다.

그래서 **코스 편집 화면(2단계)에는 한적도를 노출하지 않는다.** 후보 목록의 배지도, 지도 마커 색도.
점수를 미리 보여주면 "직접 짠 코스"가 아니라 시스템이 유도한 코스가 되어, 진단의 의미가 사라진다.

---

## 점수 체계 (⚠️ 설계 시 최우선 주의)

점수는 **두 개뿐이다.** 한적도(원본 지표)와 추천도(종합 판단).
셋째 점수를 만들지 말 것 — 화면에 없는 값으로 목록을 줄 세우면 "왜 이게 1등인지"를 설명할 수 없다.

### 한적도
공사 집중률 예측 데이터 기반. 해당 장소가 해당 날짜에 얼마나 덜 붐빌지.
높을수록 한적. 서비스의 핵심 지표.

진단 화면의 모든 장소에 표시하고, 대안 목록에서도 **판단의 원본 수치로 함께 노출**한다.
장소와 날짜만 있으면 계산되므로 비교 대상 없이도 존재한다.

⚠️ **3단계 경계는 `65 / 35`다** (`CongestionLevel`, 2026-08-31 확정).
전국 16개 시도에서 뽑은 **64개 시군구 · 관광지 2,905곳 · 관측 87,150건**으로 잡았다.
서비스 지역 셋만 보고 잡으면 안 된다 — **우리 지역이 전국에서 붐비는 축이라서**,
그 셋의 분위수로 잡은 값(63/30)은 지역을 늘리는 순간 전국의 45%가 "한적"이 되어 무너진다.
- 시도별 한적도 중앙값이 서울 37.4 · 제주 46.7부터 경남 68.6 · 강원 67.6까지 **32점** 벌어져 있다.
  **하나의 절대 경계로 전국과 서비스 지역을 동시에 고르게 가를 수 없다** — 격자 탐색으로
  확인했고, 최적조차 "가장 불리한 시도의 최소 배지 16.7%"였다. 그러니 이 값은 실측 분위수가
  아니라 **두 조건을 함께 만족하는 절충**이라고 말해야 한다. 분위수로 잡는 방법(추천도 구간이
  쓴 방법)은 3개 지역만 볼 때만 성립했다
- **한 배지가 45%를 넘지 않게 둔다.** 넘으면 그 배지가 기본값이 되어 신호를 잃는다
- ⚠️ **경계를 옮기면 `CrowdSensitivity`의 "붐비는 곳 제외" 범위도 함께 움직인다.**
  둘은 같은 자를 나눠 쓴다
- ⚠️ **홈 주간 예보가 제주에서 안 갈리는 것은 이 경계 문제가 아니다.** 평균이 폭을 죽이고
  (서귀포 원자료 4.6~86 → 대표 50곳 평균 38~49), 요일 효과가 지역마다 다르다
  (경주 40점 vs 서귀포 8점). 경계로 고치려 들지 말 것 — `docs/OPEN_DECISIONS.md` 11-1-1

### 추천도
**"이 서비스가 그곳을 대안으로 얼마나 미는가"를 나타내는 점수.** 교체 추천 시에만 사용.
원래 장소가 있어야 성립하는 **관계값**이라, 진단 화면 타임라인에는 나오지 않는다.

⚠️ **한적도를 추천도에 포함한다.**
한적한 곳으로 사람을 보내는 것이 추천의 목적 자체다. 붐비는 곳을 "좋은 대안"이라 부를 수 없다.
**반드시 한적도의 반영 비율을 가장 높게 둔다.** 그래야 서비스 정체성과 과제 부합이 유지된다.

⚠️ **추천도를 단순 인기도로 정의하면 안 된다.**
인기 장소 = 붐비는 장소이므로, 인기도를 가점으로 쓰면 오버투어리즘 과제와 정면으로 모순된다.
심사에서 반드시 지적받는 지점.

추천도의 **점수 항목은 둘이다 — 한적도와 동선 근접도.** 나머지는 점수가 아니라
**후보를 거르는 문**이다. 층이 다르므로 섞어 적지 말 것.

**점수로 계산하는 것:**
- **한적도** (반영 비율 최대)
- **동선 근접도**

**후보를 거르는 것 (점수 아님):**
- **연관성** (연관 관광지 API — 함께 많이 방문되는 곳). 후보를 만드는 출발점이자
  인기도 하한을 겸한다
  - ⚠️ **점수 항목으로 넣지 않는다** (2026-08-31 실측). 넣으려고 재봤더니 연관 순위와
    한적도가 **음의 상관**이었다 — 6개 지역 26,819쌍에서 켄달 타우 **−0.073**,
    6곳 중 5곳이 음수(서귀포 −0.139 · 경주 −0.127). **함께 많이 가는 곳일수록 더 붐빈다.**
    당연한 결과다, 연관 관광지는 곧 인기도이고 인기 = 붐빔이다. 가점을 주면
    "더 붐비는 곳을 더 밀어라"가 되어 과제와 정면으로 어긋난다.
    음의 상관이 **가장 강한 곳이 하필 경주·서귀포**라 더 그렇다
  - 대신 **화면이 그 사실을 말한다** — 구성 내역 아래 "후보를 고른 방법" 한 줄
    (`CandidateSource.noteFor`). 문장은 서버가 만든다. 명세와 화면을 나란히 놓았을 때
    항목이 빠진 것처럼 보이던 자리를 이 줄이 채운다
  - ⚠️ `ScoreFactor`로 넣지 말 것. 항목 목록 안에 끼우면 점수 없는 항목이 되어
    "계산했는데 0점"으로 읽힌다
- **카테고리 적합성** — 대분류로 뭉뚱그리지 않고 **중분류(`lclsSystm2`)로 가른다.**
  대분류만 보면 `VE`가 박물관·워터파크·리조트를 한데 묶어 **황리단길 자리에 리조트**가 올라온다
  - 역사·유적(HS) ↔ HS · 박물관·공연장·책방(VE07/06/12)
  - 자연·풍경(NA) ↔ NA · 전망대·공원(VE01/03)
  - 문화·명소(VE) ↔ 관광 대상인 VE 전부 / 제외: 리조트·관광단지(VE05) · 도서관(VE09) · 수련관(VE10)
  - 그 밖은 같은 대분류끼리만. **양방향으로 맞춘다** — 유적에서 박물관으로 갈 수 있으면 반대도 되어야
    한다. 한쪽만 열면 같은 두 장소가 어느 쪽을 누르느냐에 따라 다른 답을 준다
- 인기도는 가점이 아니라 **"너무 무명은 아니다" 수준의 하한 필터**로만 사용
  - 연관 관광지 API가 이 역할을 겸한다 — 아무도 함께 가지 않는 곳은 연관 목록에 나오지 않는다
  - ⚠️ **다만 연관 후보만으로는 자주 빈다.** 실측(2026-08-25)에서 관광지 넷 중 셋이
    대안을 얻지 못했다(경주 25.6% · 제주시 30.9% · 서귀포 33.7%). 연관 후보가
    **하나도 없을 때만** 같은 지역 카탈로그로 넘어간다(74~88%로 오른다)
  - 그때는 인기도 하한이 사라지므로 **나머지 조건(분류·거리·한적도)을 그대로 지킨다.**
    두 출처를 한 Pool에 섞지 않는다 — 섞으면 같은 목록에서 어떤 줄은 "함께 많이 찾는 곳"이고
    어떤 줄은 아니게 되어 근거 문구가 정직해질 수 없다
  - **근거 문구는 출처에 따라 갈린다.** 지역에서 고른 후보에게 "함께 많이 찾는 곳"이라 하면
    계산하지 않은 것을 근거로 말하는 것이다 → "OO와 비슷한 분류의 가까운 곳"

**대안 후보를 추천도 순으로 줄 세우지 않는다** (2026-08-26 확정). 정렬 기준이 화면에 보이는
값이어야 한다는 요구는 유효하지만, **점수순 정렬이 추천 분산을 통째로 죽인다** — 뽑기 결과를
점수로 다시 덮으면 최고점이 언제나 1등이 되어 1등 고정률이 68~82%로 뛴다.
아래 [추천 분산](#추천-분산)이 이 규칙을 대신한다.

**대안이 되려면 원래 자리보다 한적해야 한다.** `MIN_QUIETNESS_GAIN = 5` —
추천도에 근접도가 섞여 있어, 이 하한이 없으면 **아주 가까운 곳이 더 붐비는데도** 총점이 높아
"대안"으로 나간다. 붐빔을 피하라는 서비스가 더 붐비는 곳을 권하게 된다.
거리 상한은 `MAX_DISTANCE_KM = 15`.

⚠️ 하한 때문에 **목록이 비는 일이 흔하다.** 그때 빈 화면으로 뭉개지 말고 **왜 비었는지**를
함께 내려보낸다(`PlaceOffStatus`) — 사라지는 자리의 대부분은 원래 자리가 이미 한적한 곳이라,
같은 빈 화면으로 뭉개면 잘 고른 사람에게 사과하는 꼴이 된다.

⚠️ **추천도는 100점 만점이 아니다.** 실측 분포는 25~80 · 중앙 53이다(150건, 2026-08-29).
`0.7 × 한적도 + 0.3 × 근접도`라 100은 구조상 나올 수 없고, 중앙값 53은 그대로 두면
낙제로 읽힌다. 그래서 화면은 **숫자를 구간 문구로 옮겨** 세운다 — 경계는 임의의 70/50이 아니라
실측의 1·3분위수(46 / 64)다. 낮은 구간에도 깎아내리는 말을 붙이지 않는다.
위 하한을 통과한 후보만 내려오므로 "원래 계획보단 한적하다"는 보장된 사실이다.

### 추천도 구성 내역 (필수)
추천도는 숫자 하나로 내려보내지 않는다. **항목별 점수·반영 비율·근거를 함께 내려 화면에 편다.**

```
추천도 73
  한적도      76   반영 70%   같은 날 예상 혼잡이 낮다
  동선 근접도  67   반영 30%   원래 자리에서 4.2km
```

합친 점수는 결과만으로 근거가 사라진다. 심사위원의 "추천도를 어떻게 산출하나요?"에
화면을 가리켜 답할 수 있어야 한다 — **데이터 활용 20점을 증명하는 장치다.**

**내역은 펼쳐보기 안에 세로 목록으로 편다.** 한 줄 수식(`추천도 73 = 한적도 76 (70%) + …`)으로
카드 겉면에 폈다가 걷어냈다 — 좁은 화면에서 항목·점수·비율이 제각기 줄을 바꿔
**수식으로 읽히라고 만든 줄이 조각 더미**가 됐다. 가로로 이으려던 것을 세로로 세우면
폭과 싸울 일이 없다.

⚠️ **항목 이름·점수·비율을 화면에 문자열로 박지 말 것.** 전부 서버가 준 `factors`에서 그린다 —
설문의 혼잡 민감도에 따라 `ScoreWeights`가 실제로 달라지고, 항목이 늘어도 따라온다.
다만 `factors`가 비어도 **합계는 그린다.** 바깥에서 걸러내면 구버전 서버가 내역을 안 줄 때
추천도 숫자가 화면에서 통째로 사라진다.

**반영 비율은 서버가 내려보낸다.** 화면에 비율을 적어두면 분석 결과로 가중치가 바뀔 때
한쪽만 고쳐져 두 값이 어긋난다. 임계값을 서버에 두는 것과 같은 이유.

### 추천 분산
동일 대안이 모든 사용자에게 반복 추천되면 그곳이 새로운 혼잡지가 된다 — 2차 오버투어리즘.
추천도 상위 후보군에서 최종 노출은 **가중 무작위 추출**로 선택한다.
점수가 높을수록 뽑힐 확률이 높되, 매번 같은 결과가 나오지는 않게 한다.

**장소 교체 추천에도 적용한다.** 설문 코스 생성만이 아니다 — 대안 추천이야말로
"모두를 같은 곳으로 보내는" 자리다.

**순서를 지킨다. 거르기가 먼저이고 뽑기가 마지막이다:**

1. 지역·세부분류·혼잡자료·좌표·중복 조건을 통과한 후보만 남긴다
2. 한적도와 직선거리 근접도로 추천도를 계산한다
3. 일정 점수 이상의 **상위 후보 Pool**을 만든다
4. Pool 안에서 점수에 비례한 가중 무작위로, 중복 없이 뽑는다
5. 뽑힌 목록을 **코스 편집 세션에 저장**한다

거르기를 뽑기 뒤로 미루면 자격 미달 후보가 무작위로 1등이 될 수 있다.
Pool을 먼저 자르기 때문에 뽑히는 것은 언제나 "충분히 좋은 후보" 안에서다.

⚠️ **뽑은 뒤에 점수순으로 다시 정렬하지 말 것** (2026-08-26 실측). 이 장치가 오래도록
**아무 일도 하지 않고 있었다** — 같은 자리를 40번 물어도 1등이 한 번도 바뀌지 않았다.
값(Pool 3 · 가중 지수 1.2)은 전부 맞게 들어 있었고, 죽인 것은 우리 구현 두 가지였다:

1. **화면이 8개를 요청했다.** Pool이 셋인데 여덟을 달라고 하면 "다 가져가라"와 같아
   Pool이라는 개념이 무의미해진다 — 화면도 **Pool 크기만큼만** 요청한다
2. **뽑은 뒤 추천도 순으로 다시 정렬했다.** 뽑힌 순서가 통째로 덮여 최고점이 언제나 1등이 됐다

고친 뒤 1등이 3종류씩 돌아간다(고정률 95~100% → 38~42%, 이론값 ≈35%).

**정렬이 필요하면 구간 단위까지만 한다** (2026-08-30). 화면이 점수를 구간 문구로 옮겨
세우면서, "(문구 없음)" 위에 있고 "이날 가기 좋아요"가 그 아래 서는 것이 고장으로 읽혔다.
구간까지만 세우면 **줄 세운 값이 카드에 적힌 문구 그 자체**라 설명이 서고,
**같은 구간 안은 뽑힌 차례 그대로**라 분산도 산다.

⚠️ **화면 문구와 한 몸이다.** "추천도가 높은 순이에요" 같은 문구를 두면 화면이 거짓말을 한다.
둘 중 하나만 되돌리면 안 된다.

⚠️ **재추첨 조건을 지킨다.** 사용자가 새 추천을 **명시적으로 요청**했거나
**코스·날짜·지역 조건이 바뀐** 경우에만 다시 뽑는다.
**화면이 다시 그려질 때마다 다시 뽑지 않는다** — 대안 시트를 닫았다 열었을 뿐인데 목록이
바뀌면 사용자가 **되돌아갈 후보를 찾지 못한다.** 5번의 세션 저장이 이것을 막는 장치다.

⚠️ **캐시는 원자료 층에만 둔다.** 서버가 완성된 대안 목록을 캐시해 모든 사용자에게 돌려주면
분산이 통째로 죽는다. 공사 응답(집중률·연관 관광지)까지만 캐시하고, 점수 계산과 뽑기는 매번 한다.

구현 시 그 이유를 주석으로 남길 것. 기능설명서와 PT 답변의 근거가 된다.

### 코스 총점
**슬롯 한적도의 평균.** 추천도가 섞이지 않는다.
원안과 개선안을 맞대는 값이라(PT 하이라이트), 순수 한적도로 두어야 개선폭이 그대로 읽힌다.

**숫자를 내걸려면 근거가 두꺼워야 한다** (`CourseScoreStandard`):

```
진단 관광지 ≥ 2  AND  진단 관광지 / 예측대상 관광지 ≥ 0.50
```

- 못 채우면 숫자 대신 **등급 요약**을 편다 — `한적 2곳 · 보통 1곳 · 자료 없음 3곳`.
  평균이 아니라 사실의 나열이라 근거가 얇아도 정직하다
- 채우면 **모수를 함께 적는다** — "관광지 3곳 중 2곳의 예측자료 기준".
  `"전체 코스 혼잡도"`라는 명칭은 쓰지 않는다
- 원안 vs 개선안은 **양쪽 다 보여줄 수 있을 때만** 견준다
- ⚠️ **이 조건을 저장과 묶지 말 것.** 묶으면 경주 코스의 41.7%가 저장 불가가 된다
  (지역별 코스 120개 시뮬레이션, 2026-08-26). 경주는 파일럿 지역이고 심사에서 가장 많이 쓸 곳이다

나머지 임계값·가중치는 `docs/OPEN_DECISIONS.md`에 어디 있는지 모아 두었다.

---

## 활용 공사 OpenAPI

| 데이터 | 역할 | 쓰는 곳 | 상태 |
|---|---|---|---|
| 국문 관광정보 (TourAPI) | 지역 관광지 기본 Pool + 장소 상세 — 콘텐츠 ID·이름·분류·좌표·사진 | 검색, 전 기능 | 사용 |
| 관광지 집중률 방문자 추이 예측 | **한적도의 원천** — 방문일별 한적 지수 | 전 기능 | 사용 |
| 중심 관광지 | 코스 자동 생성의 첫 장소 후보 · 검색 전 대표 관광지 | FULL, 검색 | 사용 |
| 관광지별 연관 관광지 정보 (티맵 기반) | 대안 후보 생성 — **기준 장소가 있을 때만** 성립 | PLACE, FULL | 사용 |
| Kakao Local | 좌표 보완, 지도 표시 | 보조 | 사용 |
| 지역별 방문자수 빅데이터 | 지역 단위 쏠림 검증·보조 지표 | — | **현재 미사용** |

> ⚠️ 이 데이터는 **실시간이 아니라 예측·통계값**이다.
> UI 문구는 "실시간 혼잡"이 아니라 **"예상 혼잡 / 예측 기반"**으로 표현할 것.

**"현재 미사용"은 지우지 않고 남긴다.** 계산에 넣지 않은 데이터를 근거로 말할 수는 없으므로
화면과 기능설명서에서는 빼되, 나중에 붙일 자리로 표에는 남겨 둔다.

**장소는 키워드 검색으로 찾는다.** 지역 전체 목록을 내려받지 않는다 — 지역이 늘어나면
목록 UI가 성립하지 않는다. 검색은 선택한 지역 내부로 제한하고 지역 밖 결과는 버린다.
Kakao Local은 공사 좌표가 없거나 장소 확인이 필요할 때만 보완으로 쓴다 — 공사 좌표가 우선이다.

### 엔드포인트 (실측 확인, 2026-08-21)

| 데이터 | 경로 (`https://apis.data.go.kr/B551011/…`) |
|---|---|
| 국문 관광정보 | `KorService2/searchKeyword2` |
| 집중률 예측 | `TatsCnctrRateService/tatsCnctrRatedList` |
| 연관 관광지 | `TarRlteTarService1/areaBasedList1` |
| 중심 관광지 | `LocgoHubTarService1/areaBasedList1` |

**활용신청은 API마다 따로 한다.** 포털 계정이 아니라 API 단위로 키가 등록되고,
승인 표시가 뜬 뒤에도 게이트웨이 반영에 1~2시간 걸린다.

> 💡 **경로를 찾는 법**: 응답의 오류 코드가 갈린다.
> `30`(SERVICE_KEY_IS_NOT_REGISTERED)은 **경로는 맞고 키만 미등록**,
> `12`(NO_OPENAPI_SERVICE)는 **그런 경로가 없다**는 뜻이다.
> 오퍼레이션 이름이 틀려도 12가 나오므로, 서비스명과 오퍼레이션을 함께 맞춰야 30이 뜬다.

### 집중률 API — 실측으로 확인한 제약 (2026-08-21)

```
GET https://apis.data.go.kr/B551011/TatsCnctrRateService/tatsCnctrRatedList
    ?areaCd=47&signguCd=47130   ← 둘 다 필수 (경북/경주)
    &tAtsNm=불국사               ← 선택, 부분일치
응답: baseYmd · areaCd/areaNm · signguCd/signguNm · tAtsNm · cnctrRate
```

- **예측 범위는 조회 시점부터 앞으로 24~30일이고 과거 날짜는 없다.**
  ⚠️ **길이가 고정이 아니다** — 24일(2026-08-21 관측)이었다가 30일(2026-08-30 관측)이 됐다.
  숫자를 코드에 박지 말 것. 서버가 실제 응답에서 마지막 날을 읽어 내려준다
  (`ForecastWindowResponse`). 범위 밖은 "자료 부족"으로 다룬다
- ⚠️ **창 밖 날짜를 막지 않는다.** 여행은 원래 미리 계획한다 — 24일 안에 떠나는 사람만
  받겠다는 것은 대상을 스스로 좁히는 일이다. 진단은 200으로 뜨고 그 칸이
  `DATE_OUT_OF_FORECAST`("아직 예측이 나오지 않은 날짜예요")가 된다.
  날짜 고르는 자리에서 미리 알리되(`GET /api/dates/forecast-window`) `max`를 걸지 않고,
  색도 경고(붐빔)가 아니라 보통(앰버)이다 — 잘못이 아니라 "지금은 아직"이라서다
- **예측 대상은 관광지와 시장이다.** 음식점·숙박은 애초에 없다.
  쇼핑(SH)은 공사가 **시장만** 예측해 열어 두었지만(동문재래시장 28 · 서귀포매일올레시장 25),
  그 대가로 셋을 갈랐다 — **이름을 대볼 분류**(SH 포함) / **자료 없다고 말 걸 분류**(SH 제외,
  제주시 296곳 중 290곳이 "정보 없어요"를 세운다) / **포함 매칭을 허용할 분류**(SH 제외,
  `"다이소 경복궁역점" → "경복궁"`은 좌표 2km 검사로도 못 막는다)
- ⚠️ **같은 날짜의 예측값도 갱신되면서 바뀐다.** 요일 패턴을 그대로 돌려주는 표가 아니라
  날짜마다 따로 매겨진 예보다 — 경주 69곳을 재보니 30일 창에서 **장소마다 서로 다른 값이
  중앙 30개**였고, `d`와 `d+7`의 값이 같은 경우가 **1,587쌍 중 0쌍**이었다(2026-08-30).
  요일 효과는 크지만(토 71.4 · 수 31.3) 같은 토요일끼리도 주마다 다르다(71.0/69.0/68.8/76.6).
  저장 코스에 점수 스냅샷을 남기고 열 때마다 재계산하지 않는 이유가 이것이다.
  실제 변화폭은 `analysis/forecast-drift/snapshot.py`로 잰다
- **`cnctrRate`는 0~100이고 높을수록 붐빈다.** 한적도는 이 값을 뒤집어 만든다.
  변환식은 분석 담당이 실제 분포로 확정한다
- ⚠️ **관광지 식별자가 이름 문자열(`tAtsNm`)뿐이다.** 콘텐츠 ID가 없어 국문 관광정보와는
  **이름 매칭**으로만 이을 수 있다. 값이 `"경주 불국사 [유네스코 세계유산]"` 형태라
  괄호·지자체명·공백 정규화가 필수다. **여러 후보가 나오면 자동으로 확정하지 않는다** —
  잘못 이으면 다른 장소의 혼잡도를 그 장소의 것이라고 말하게 된다. 이번 연동의 최대 위험

외부 지도 API(카카오맵 등)는 자유롭게 사용 가능.

---

## 필수 구현 기능

우선순위 순. 위에서부터 반드시 완성한다.

1. **코스 편집** — 지도 기반으로 일자별 장소 추가·삭제·순서 변경
   - v1은 순서 변경을 **위/아래 버튼**으로. 드래그앤드롭은 여유 시 업그레이드
   - 장소는 **키워드 검색**으로 찾는다. 지역 전체 목록을 한 번에 내려받지 않는다 —
     지역이 늘어나면 한 화면에 담을 수 없고, 목록을 훑는 화면은 확장되지 않는다
   - 검색 범위는 **선택한 지역 내부로 제한**한다
   - 검색 전 빈 화면에는 **대표 관광지 몇 개를 칩으로** 제시한다 (중심 관광지 API).
     빈 검색창만 두면 경주를 모르는 사용자가 첫 글자를 못 친다
2. **한적도 진단** — 코스 완성 후 각 장소·날짜의 한적도 계산 및 시각화 (3단계 배지 등)
3. **장소 교체 추천** — 상위 Pool에서 가중 무작위로 뽑은 셋. **점수순으로 줄 세우지 않는다**
   (위 [추천 분산](#추천-분산)). 한적도는 3단계 배지로 함께 표시
4. **추천 근거 노출** — 모든 추천에 이유를 함께 표시 (**데이터 활용 20점을 화면에서 증명하는 장치**)
   - 문장 하나로 끝내지 말고 **추천도 구성 내역**(항목·점수·반영 비율)을 함께 편다
   - 예: "OO 방문객이 함께 많이 찾는 곳 · 같은 날 예상 혼잡은 OO의 40% 수준"
   - ⚠️ **계산하지 않은 것을 근거로 말하지 않는다.** 연관 관광지 데이터를 붙이기 전에는
     "함께 많이 찾는 곳"이라 쓸 수 없다. 근거 문구는 실제 계산한 항목만 반영한다
5. **날짜 대안 제시** — 선택 날짜보다 한적한 날짜 안내
   - 접수 시 제출한 서비스 개요의 "두 가지 회피 경로(날짜/장소)" 중 하나. 반드시 포함
   - 구현 비용 낮고 발표 가치 높음
6. **설문 기반 코스 추천** — 일정 밀도·혼잡 민감도 **2문항**을 받아
   분산 로직 기반으로 코스 초안을 생성
   - 경주를 모르는 사용자의 진입점. 현재 흐름은 지역을 아는 사람만 쓸 수 있다
   - 사후 교정이 아니라 **사전 분산 유도** — 과제 해결 측면에서 강력하다
   - 기존 한적도·연관성 로직을 재사용하므로 추가 데이터가 필요 없다
   - ⚠️ **문항을 늘릴 때 지킬 것: 어느 답을 골라도 코스가 나와야 한다.**
     고른 대가로 결과가 비는 문항은 선택지가 아니라 함정이다.
     4문항이었다가 둘을 걷어낸 이유가 이것이다(2026-08-27) —
     **여행 스타일**(역사·자연·문화)은 하나만 고르면 후보가 제주시 3곳·서귀포 2곳으로
     쪼그라들었고, **이동수단**(자차/대중교통)은 대중교통을 고르면 반경 8km 밖이 통째로 잘렸다.
     둘 다 "고른 답이 후보를 거른다"는 같은 병이다
   - 지금은 서버가 **코스에 어울리지 않는 분류만 뺀다**
     (`PlaceCategories.isCourseCandidate` — 음식점·숙박·축제, 그리고 문화·명소에 섞여 있는
     리조트·도서관·수련관). 거리 제한은 넉넉한 쪽 하나로 고정
     (`CourseDraftService.DAY_RADIUS_KM`)
   - 남은 두 문항은 후보를 거르지 않는다. 밀도는 슬롯 수만, 민감도는 점수 비중과 하한만 바꾼다
7. **원안 vs 개선안 비교** — 최초 코스 총점과 교체 후 총점을 나란히 제시 (**PT 하이라이트**)
8. **코스 저장·비교** — 회원 계정에 저장, 저장된 코스 간 비교

### 보류 항목
- **시간대 조정 제안** — 장소를 유지하되 한산한 시간대 방문 권유.
  공사 데이터에 시간대별 혼잡 정보가 있는지 확인되지 않았다.
  실측에서 확인되면 진행하고, 없으면 만들지 않는다.
  (근거 없는 시간대 추천은 심사에서 지적받는다)

---

## 인증 / 저장 — 단계적 구현

**순서를 반드시 지킬 것.** 아래 층이 완성되기 전에 위층으로 가지 않는다.

- **1층 (최우선)**: 게스트 모드로 전체 흐름 완성. 로그인 없이 코스 편집·진단·교체까지 가능.
- **2층**: 이메일/비밀번호 로그인 + JWT. 회원의 코스 저장 및 코스 간 비교.
- **3층 (여유 시)**: 소셜 로그인(카카오/네이버). 외부 등록·검수 소요로 후순위.

게스트는 1회성으로 서비스 전체를 체험할 수 있어야 한다. 로그인은 "코스를 저장하고 비교하기 위한" 선택지이지 진입 장벽이 아니다.
코드는 처음부터 사용자 식별 부분을 분리해 인증을 나중에 얹기 쉽게 설계할 것.

### 저장 코스 처리
저장은 **재료만** 한다: 지역, 날짜, 박 수, 장소 ID와 순서, 그리고 저장 시점의 점수 스냅샷.
불러올 때 스냅샷을 먼저 보여주고, 사용자가 "다시 진단하기"를 누르면 그때 재계산한다.
- 예측 데이터가 갱신되므로 열 때마다 자동 재계산하면 숫자가 흔들려 혼란스럽다
- 여행 날짜가 지난 코스는 예측 데이터 범위 밖이므로 재계산하지 않고 "지난 여행"으로 표시한다

**총점이 없는 코스가 존재한다.** 밥집만 담은 코스, 그리고 예측 창 밖 날짜로 짠 코스가 그렇다.
그때 코스 총점은 `null`이고, **0으로 채우지 않는다.** 0은 화면에서 "매우 붐빔"으로 읽혀
밥집만 담았다는 이유로 최악의 코스라고 말하게 된다.
- 진단 화면은 **뜬다.** 점수 자리만 비우고 장소·순서·지도는 그대로 그린다 —
  총점이 없는 것과 코스가 없는 것은 다르다
- **저장도 막지 않는다** (2026-08-26). 저장은 <b>재료</b>를 남기는 일이고 점수 스냅샷은
  있으면 함께 남기는 것이다. 없다고 재료까지 못 남길 이유가 없다 —
  "미리 짜 두고 나중에 진단"이 예전 규칙에 막혀 있었다
  - `SavedCourse.totalQuietness`는 `Integer`(nullable)다. `scoredAt`도 함께 null이 된다 —
    매긴 적이 없는데 시각만 남기면 거짓말이다(저장 시각은 `createdAt`이 따로 갖는다)
  - 홈의 "다른 사람들의 여행"에서는 **진단 안 된 코스를 뺀다.** 거르기는 `limit` **앞**이다
  - 마이페이지 평균은 **진단된 코스만**으로 낸다. 0으로 세면 저장만 해도 평균이 떨어진다
  - 카드는 점수 자리에 `—`, 등급 자리에 "아직 진단 전"
- 예측이 없는 장소에도 **장소 교체는 열어 둔다.** 한적도를 모르면 추천도를 매길 수 없으므로
  추천이 아니라 "같은 분류 · 직선거리"만 전하는 별도 경로다 (`GET /places/{id}/nearby`).
  ⚠️ 대안 추천과 **엔드포인트를 합치지 말 것** — 점수 자리가 빈 응답은 화면이
  "아직 점수가 안 온 추천"으로 읽는다

### 남의 코스를 공개하는 범위
홈의 "다른 사람들의 여행"은 **코스 id와 저장한 사람을 내보내지 않는다.**
id를 주면 번호를 훑어 남의 코스를 하나씩 여는 통로가 된다.

**공개 여부를 사용자가 고른다** (2026-08-27). 저장 시트의 토글이 정하고,
공개로 고른 코스만 홈 목록에 나간다. 이름도 그때 함께 나간다.
- 지역과 기간만으로는 어느 카드나 "경주 1박 2일"이라 서로 구분되지 않았다 —
  이름이 있어야 남의 여행이 남의 여행답게 읽힌다
- 그렇다고 이름만 공개하면 **동의한 적 없는 것**을 내보내게 된다.
  감추기도 알리기도 아닌 **고르게 하기**가 둘 다 푼다
- 기본값은 켜짐. 토글이 이름 바로 아래 있어 저장 전에 눈에 들어오고,
  아무도 공개하지 않으면 그 목록이 늘 비어 아무에게도 쓸모가 없다
- ⚠️ **고른 적 없는 옛 코스는 비공개로 읽는다**(`SavedCourse.isPublic`).
  묻지 않고 내보내지 않기 위해서다
- 펼쳐 보기가 필요해도 **상세 엔드포인트를 새로 열지 말 것.** 목록 응답에 장소를 전부 실으면
  주소 없이 내용만 나가고, 누를 때 추가 호출도 없다
- 장소의 `placeId`는 나간다 — 공사 콘텐츠 ID라 공개 값이고, "이 코스로 나도 짜보기"에 필요하다

---

## 기술 스택

- **백엔드**: Java + Spring Boot
- **프론트엔드**: 웹, **모바일 우선 반응형** (390px 기준 설계, 데스크톱에서 깨지지 않게)
- **분석**: Python / Jupyter Notebook (검증용)
- **앱 개발 안 함.** 웹만. (앱은 스토어 등록·승인 기한 리스크)

### 분석 로직 처리 방식
파이썬 노트북은 **검증용 연구실**이며 서비스에 직접 붙지 않는다.
노트북에서 확정된 **규칙(임계값, 점수식, 가중치)을 자바로 이식**해 서버에 심는다.
별도 파이썬 API 서버를 띄우지 않는다.

---

## 범위 밖 (만들지 말 것)

요청받지 않는 한 제안하지도 말 것.

- 블루/그린 무중단 배포, Docker 오케스트레이션
- Prometheus/Grafana 모니터링, 부하 테스트
- 고급 DB 최적화(Materialized View 등), 마이크로서비스 분리
- 사용자 실시간 위치(GPS) 기능
- ~~다중 지역 지원~~ — 2026-08-24에 제주시·서귀포시를 넣었다. 제주 집중률이 448곳이라
  경주(69곳)로는 자주 비던 대안 후보가 훨씬 넉넉해진다. **더 늘리지는 말 것**
- 최단 경로 최적화 알고리즘 (근접도 기반 정렬로 충분)
- 커뮤니티 기능 (리뷰·댓글·게시판) — 사용자가 없으면 빈 껍데기가 된다. 확장 계획으로만 언급

배포는 "심사위원이 URL로 접속해 안정적으로 사용 가능한 상태"면 충분하다.

---

## 저장소 구조

```
peakoff/
├── backend/            # Spring Boot
├── frontend/           # 웹 (모바일 우선 반응형)
├── analysis/           # 파이썬 노트북 (검증용)
├── .github/workflows/  # CI: 빌드 확인 위주
└── CLAUDE.md
```

백엔드는 도메인별 패키지 분리. 외부 API 연동은 별도 `external` 패키지로 분리.
예: `course/`, `congestion/`(한적도), `recommendation/`(추천도·대안), `place/`, `external/`, `global/`

---

## 작업 방식

- **백엔드 코드를 작성하거나 수정하기 전에 반드시 무엇을 어떻게 바꿀지 먼저 설명하고 승인을 받는다.**
- **한 번에 큰 덩어리를 만들지 말 것.** 기능 하나씩 작게 쪼개 구현하고, 매번 실제 동작을 확인한 뒤 다음으로 넘어간다.
- 코드 작성 후에는 **무엇을 왜 그렇게 짰는지 설명**할 것. 개발자가 최종 발표(PT)에서 직접 설명해야 한다.
- 인증키·시크릿은 **환경변수**로 처리. 코드에 하드코딩하거나 커밋하지 않는다.
- 기능이 하나 동작할 때마다 커밋한다. (개발 이력 자체가 심사 자료)
- **테스트 코드는 요청할 때만 작성한다.** 기본적으로 구현 코드만 작성할 것.
  단, 점수 계산 로직과 도메인 검증은 예외로 간단한 단위 테스트를 포함한다.

---

## 프론트엔드 디자인 규칙

- 모바일 우선. 390px 기준 설계, 데스크톱에서 깨지지 않게 반응형
- **팔레트의 원천은 넉 줄짜리 색 띠 한 장이다** — 네이비 `#364F6B` · 틸 `#3FC1C9` ·
  회백 `#F5F5F5` · 핑크 `#FC5185`. **네 색에 각각 자리를 하나씩** 주었고 겹치는 색이 없다:

  | 원색 | 자리 | 토큰 |
  |---|---|---|
  | 네이비 `#364F6B` | 잉크 (글자·어두운 면) | `--c-fg: #2A3E54` (한 단계 내림) |
  | 틸 `#3FC1C9` | 브랜드 · **지정 메인 색** | `--c-brand` (원색 그대로) |
  | 회백 `#F5F5F5` | 화면 바탕 | `--c-bg` (원색 그대로) |
  | 핑크 `#FC5185` | 붐빔 | 정체성 자리(띠·글로우)에 원색, 기능 단계는 파생 |

  - 원색들이 이미 진해서 램프의 방향이 파스텔 때와 반대다 — 원색이 point·solid급이고,
    tint·soft를 위로 늘려 만든다
  - 네이비 원색은 틸 버튼 위에서 3.9:1이라 잉크로는 한 단계 내린 `#2A3E54`를 쓴다.
    글자·어두운 면·그림자(`rgb(42 62 84 / α)`)가 전부 이 네이비 기운을 공유한다
  - 핑크 원색은 휘도 0.28로 한적 solid와 명도 서열이 겹쳐, 기능 단계는 어둡게 내렸다.
    원색 그대로는 띠·글로우에만 산다
  - 카드는 순백 `--c-surface: #FFFFFF`. 회백 바탕은 중립색이라 크림을 얹으면 누렇게 뜬다
- **정체성 장치 세 가지** — 색을 토큰으로 흩는 데서 끝내지 않는다:
  - ~~팔레트 띠 모티프~~ — **걷어냈다.** 원색 셋을 이은 2~3px 띠를 여러 화면에 두르다가
    마지막 남은 결과 히어로에서도 뺐다(2026-08-24). 정체성은 아래 셋이 맡는다 —
    얇은 삼색 막대가 하나 더 있는 것보다 그쪽이 조용하다. **다시 넣지 말 것**
  - **로고 마크**: 봉우리(Λ)의 오른쪽 다리가 바닥까지 오지 않고 중간에서 끊기고,
    끊긴 자리에서 **한 칸 옆으로 비껴간 조각**이 브랜드 틸로 남는다.
    `PEAK`(혼잡의 정점)와 `OFF`(낮추어 비껴가다)를 그대로 그림으로 옮긴 것이다
    - 정의는 `components/BrandMark.tsx` **한 곳**. 네 화면(헤더 둘·로그인·가입 패널)이
      같은 것을 각자 그리다가 모았다 — 하나를 빠뜨리면 화면마다 다른 로고가 선다
    - 글자는 **`PEAKOFF`**로 붙여 쓴다 (2026-08-30). 서비스 이름이 한 낱말이고,
      `PEAK`/`OFF` 두 낱말의 뜻은 마크가 이미 그림으로 말한다. 자간이 벌어져 있어
      붙여 써도 두 덩어리로 읽힌다
    - ⚠️ **로고 원본의 파랑을 그대로 옮기지 말 것.** 색의 원천은 `--c-*` 한 곳이고,
      서비스가 미는 방향(한적)은 이미 틸이 맡고 있다. 봉우리는 잉크, 비껴간 조각은 틸
    - 어두운 면에서는 **두 색이 함께 바뀐다** — 봉우리 흰색, 조각은 그린틸
      (`--c-quiet-soft`). 브랜드 틸을 그대로 두면 뒤에 깔린 틸 글로우에 묻히고
      같은 면의 틸 버튼과 무게가 겹쳐 로고가 행동 유도로 읽힌다
  - **바탕 wash**: body 맨 위 660px만 옅은 시안 기운(`--c-wash-top`)이 돌다가 회백으로
    가라앉는다 — 메인 색이 첫 화면의 공기부터 지배한다. 아래는 완전한 `--c-bg`
  - **글로우 서사**: 어두운 네이비 면의 장식 원은 틸(브랜드=한적한 방향)과 핑크(붐빔)
    두 기운이다. 행동을 청하는 자리(홈·로그인)는 틸이 위·핑크가 아래, 도착한 자리
    (결과 히어로)는 틸 하나만 남는다
- **브랜드 포인트 색 = `#3FC1C9` (틸).** 버튼·로고·강조 전용
  - **밝은 색이라 위에 올리는 글자는 흰색이 아니라 잉크(`text-fg`)다.** 흰 글자는 2.2:1로 안 보인다
  - 틸을 어둡게 내려 흰 글자를 받게 하면 메인 색이 탁한 청록이 된다 — 밝게 두고 글자를 어둡게 한다
  - 글자·테두리·초점링에는 `brand-deep`(`#156067`)을 쓴다. `brand`는 배경 전용
    (유일한 예외: 어두운 면 위의 킥커. 규칙의 근거인 대비 문제가 그 자리에서만 사라진다)
  - hover(`#3CBAC2`)는 잉크 글자가 4.5:1을 지키는 한계까지만 어둡다. 더 내리면 글자가 무너진다
- **혼잡 3단계는 브랜드와 별개다** (적용값 기준):
  - 한적 = 그린틸 `#14A07C` (배경 `#DEF5E4`) / 보통 = 앰버 `#E9AE3E` (배경 `#FBF0D2`) /
    붐빔 = 핑크레드 `#E82C6E` (배경 `#FDDFE9`)
  - 세 단계는 색조에 더해 **명도로도 갈린다** (보통 0.48 > 한적 0.27 > 붐빔 0.20) —
    색각 이상에서도 읽히게 하기 위해서다
  - ⚠️ **브랜드 틸과 한적 그린틸이 같은 방향의 색이다.** 서비스가 미는 방향(한적)과
    브랜드가 한 기운인 것은 서사상 의도이지만, 등급 신호로는 갈려야 한다 — 한적은
    초록 쪽으로 밀고 명도를 내렸으며 tint도 시안 쪽/초록 쪽으로 벌려 두었다.
    한쪽만 조정하면 "브랜드 칩"과 "한적 배지"가 겹쳐 보인다
  - 보통(앰버)만 띠 밖에서 왔다. 신호등의 중간색은 앰버 말고 대안이 없고,
    브랜드가 시안이라 노랑과 부딪히지 않는다
  - ⚠️ 이 solid들은 밝아서 **흰 글자를 받지 못한다.** 글자가 얹히는 자리는
    배경(tint) + 진한 글자(deep) 조합이거나, 아래 `-strong`을 쓴다
- **단계마다 밝은 쪽에서 어두운 쪽으로 다섯 칸이 있다.** 자리에 맞는 칸을 고른다:
  `tint`(옅은 배경) → `soft`(보조 단계·어두운 면 위) → `solid`(글자가 안 얹히는 색 신호)
  → `strong`(색 위에 글자·번호가 얹히는 자리) → `deep`(글자색)
  - `-strong`: `--c-quiet-strong: #0E7A5F` / `--c-moderate-strong: #A46114` /
    `--c-crowded-strong: #B3175A`. 지도 마커, 순서 번호 원, 채운 경고 버튼에 쓴다.
    셋 다 흰 글자를 받으면서 명도 서열은 그대로다
  - **배경과 글자색은 항상 짝으로** 쓴다 (`LEVEL_ON_SOLID`). 배경만 갈아끼우면 글자가 묻는다
  - 지도 마커는 지도 배경 위에서 대비가 약해지므로 `-strong` + 흰 테두리를 두른다
- **긍정 신호는 brand가 아니라 quiet가 맡는다** — 강도 막대·"안전" 표시 등 등급 신호에
  브랜드색을 쓰면 강조와 등급이 섞인다 (비밀번호 강도 막대가 그 예)
- 색은 `index.css`의 `--c-*` 한 곳에서만 정의한다. 컴포넌트에 hex를 박지 않는다
  - 예외 셋. 모두 **토큰이 닿지 않는 자리**다:
    카카오·네이버 같은 외부 브랜드색 / 카카오맵이 그리는 캔버스의 경로 선 /
    `public/favicon.svg` (React 바깥이라 `--c-*`가 존재하지 않는다 — 팔레트를 고치면
    이 파일도 함께 고쳐야 한다)
  - 뜻 없는 옅은 채움(꺼진 체크박스, 보조 버튼 바탕)은 회색 hex 대신 `--c-fill`을 쓴다
- 한적도는 항상 3단계 배지(한적/보통/붐빔)로 표시
- 대비 기준: 본문 글자 4.5:1 이상. 새 색을 추가하면 짝이 되는 글자색까지 함께 계산해서 정한다
- 톤: 깨끗하고 차분한 여행 느낌, 여백 넉넉하게
- 둥근 모서리 카드 UI, 부드러운 그림자
- UI 텍스트는 한국어
- 지도는 카카오맵 사용

**모바일 브라우저와 자리를 다투지 않는다.** 실물 기기에서 깨진 것을 고치며 얻은 규칙이다:
- ⚠️ **화면 바닥에 붙는 고정 막대를 두지 말 것.** `position: fixed; bottom: 0`은 *레이아웃*
  화면의 바닥에 붙는데, 크롬 안드로이드는 도구막대가 여닫혀도 재배치되지 않도록 그 화면을
  늘 큰 크기로 잡아 둔다 — 도구막대가 뜨는 순간 우리 막대가 그 뒤로 숨는다.
  하단 이동 막대를 걷어내고 **헤더 안 토글 메뉴**로 옮긴 이유다(헤더는 sticky라 안 다툰다).
  불가피하게 남기면 `visualViewport`로 가려진 만큼 끌어올린다(`useBrowserChromeInset`)
- ⚠️ **가로로 미는 상자(`overflow-x-auto`)를 만들지 말 것.** 끝까지 민 제스처가 페이지로
  이어져 화면 전체가 옆으로 밀린다. `html`에 `overflow-x`를 걸어도 소용없다 —
  넘쳐서가 아니라 밀어서 생기는 일이다. 주간 예보를 옆으로 미는 띠에서 **세로 목록으로**
  바꿔 없앴다
  - ⚠️ **`overscroll-x-contain`은 방패가 아니다.** 최종 비교의 원안/개선안을 스냅 캐러셀로
    만들면서 이 속성을 믿었는데, **실물 아이폰에서 페이지가 그대로 밀렸다**(2026-08-30).
    body의 `overflow-x: clip`도 소용없다. 크로미움 에뮬레이션으로는 재현되지 않으니
    **마지막 확인은 실물 폰**이다
  - 옆으로 넘기는 느낌이 꼭 필요하면 **스크롤 상자가 아니라 `transform`으로** 만든다.
    손가락 이동량을 받아 `translate`로 옮기고 `touch-action: pan-y`로 세로만 브라우저에
    넘기면, 브라우저가 맡는 가로 스크롤이 아예 없어 페이지로 이어질 제스처도 없다
    (`ResultPage`의 원안/개선안). 처음 몇 px은 축을 정하지 않고 지켜봐야
    세로 스크롤을 삼키지 않는다
  - 제스처는 **화면에 보이지 않는 조작**이다. 눌러서도 넘길 수 있는 스위치를 함께 둔다
- `env(safe-area-inset-*)`은 `<meta viewport>`에 **`viewport-fit=cover`가 있어야** 값이 생긴다.
  없으면 언제나 0이라 노치 회피 여백이 아무 일도 하지 않는다
- 좁은 화면에서 한 화면에 다 못 넣으면 **스위치로 번갈아 보여준다** — 잘라내지 않는다.
  홈의 "붐빌 것 / 한적할 것", 최종 비교의 "원안 / 개선안"이 그 예다.
  피할 곳 옆에 갈 곳이 같은 무게로 서 있어야 이 서비스가 하려는 말이 완성된다.
  위아래로 쌓으면 맞대어 보라고 만든 화면에서 **스크롤로 기억해 비교**하게 된다 —
  같은 자리에서 갈아끼워야 바뀐 줄이 눈에 남는다

---

## 심사 기준 (판단이 필요할 때 이 배점을 기준으로)

**1차 심사**: 서비스 구현성 30 / 기획력 30 / 데이터 활용 적절성 20 / 발전성 20
**최종 심사(PT)**: 적정성 30 / 완성도 30 / 실용성 25 / 발표 15

기능 추가와 완성도 사이에서 고민될 때는 **완성도와 안정성**을 택한다.
"안정적으로 구동되는 완제품"이 평가의 핵심이다.