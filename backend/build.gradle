plugins {
	id 'java'
	id 'org.springframework.boot' version '4.1.0'
	id 'io.spring.dependency-management' version '1.1.7'
}

group = 'com.peakoff'
version = '0.0.1-SNAPSHOT'

java {
	toolchain {
		languageVersion = JavaLanguageVersion.of(21)
	}
}

repositories {
	mavenCentral()
}

dependencies {
	implementation 'org.springframework.boot:spring-boot-starter-webmvc'
	// 요청 DTO의 모양 검증(@NotBlank, @Min 등). 업무 규칙 검증은 도메인 생성자가 계속 맡는다.
	implementation 'org.springframework.boot:spring-boot-starter-validation'
	implementation 'org.springdoc:springdoc-openapi-starter-webmvc-ui:2.8.6'

	// 바깥 API를 호출하는 클라이언트(RestClient). 소셜 로그인이 카카오와 통신하는 데 쓴다.
	// 부트 4에서 HTTP 클라이언트 자동설정이 별도 모듈로 갈라져, webmvc 스타터만으로는
	// RestClient.Builder 빈이 만들어지지 않는다.
	implementation 'org.springframework.boot:spring-boot-starter-restclient'

	// 회원 저장. 공사 OpenAPI 데이터는 여기에 적재하지 않는다 — 회원 정보 전용이다.
	implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
	// 개발은 H2 파일, 배포는 PostgreSQL. 어느 쪽을 쓸지는 SPRING_DATASOURCE_URL이 정한다 —
	// 드라이버는 스프링이 URL을 보고 고르므로 둘 다 넣어 둔다.
	runtimeOnly 'com.h2database:h2'
	runtimeOnly 'org.postgresql:postgresql'

	// 비밀번호 해시(BCrypt)와 인증 필터 체인.
	implementation 'org.springframework.boot:spring-boot-starter-security'

	// JWT 발급·검증. api만 컴파일에 노출하고 구현체는 런타임에만 둔다 —
	// 애플리케이션 코드가 구현 클래스에 직접 손대지 못하게 막는 배치다.
	implementation 'io.jsonwebtoken:jjwt-api:0.12.6'
	runtimeOnly 'io.jsonwebtoken:jjwt-impl:0.12.6'
	runtimeOnly 'io.jsonwebtoken:jjwt-jackson:0.12.6'
	compileOnly 'org.projectlombok:lombok'
	annotationProcessor 'org.projectlombok:lombok'
	annotationProcessor 'org.springframework.boot:spring-boot-configuration-processor'
	testImplementation 'org.springframework.boot:spring-boot-starter-webmvc-test'
	testImplementation 'org.springframework.security:spring-security-test'
	testCompileOnly 'org.projectlombok:lombok'
	testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
	testAnnotationProcessor 'org.projectlombok:lombok'
}

tasks.named('test') {
	useJUnitPlatform()
}
