server:
  # Vite 프록시(vite.config.ts)가 이 포트를 바라본다. 바꾸면 양쪽 다 수정할 것.
  port: 8080

spring:
  application:
    name: peakoff
  profiles:
    # 목업 데이터로 동작한다. 공공데이터 API 구현이 붙으면 이 값을 바꿔 교체한다.
    active: mock
