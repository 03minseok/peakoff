# 이미지에 들어갈 이유가 없는 것들. 빌드 문맥이 작을수록 도커 빌드가 빨라진다.

# 빌드 산출물. 이미지 안에서 새로 만든다 —
# 여기 있는 것을 넣으면 내 컴퓨터에서 만든 jar가 배포될 수 있다.
build/
.gradle/
bin/

# ⚠️ 로컬 설정. 공사 인증키가 들어 있다.
# 이미지에 들어가면 레지스트리를 통해 키가 퍼진다. 배포 값은 환경변수로 넣는다.
src/main/resources/application-local.yml

# 로컬 H2 파일. 배포는 PostgreSQL을 쓴다.
data/

.idea/
*.iml
